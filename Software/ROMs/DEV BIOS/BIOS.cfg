MEMORY {
  ROM:          start=$8000,  size=$8000, file="%O.bin", fill=yes, fillval=$00;
}

SEGMENTS {
  KERNAL:         load=ROM,           type=ro,  start=$A000;
  CART:           load=ROM,           type=ro,  start=$C000;
  WOZMON:         load=ROM,           type=ro,  start=$FF00;
  VECTORS:        load=ROM,           type=ro,  start=$FFFA;
}